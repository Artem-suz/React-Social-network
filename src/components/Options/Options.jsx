import s from './Options.module.css'

const Options = (props) => {
    return (
        <div className={s.optionsPage}>
            <div className={s.optionsPageWrapper}>
            Settings in developing
            </div>
            
        </div>
    )
}

export default Options